# TODO: Implement Active Alerts Dashboard with Detections

## Tasks
- [x] Modify `updateAlerts` function in `assets/index.js` to always include detections in the Active Alerts list
- [x] Modify `handleDetectionAlertAction` function in `assets/index.js` to show emergency modal when a detection is accepted
- [ ] Test the changes to ensure detections appear in Active Alerts with Accept/Decline buttons and emergency modal on accept
